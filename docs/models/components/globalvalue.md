# GlobalValue

A global metric value that reflects the overall performance of the specified metric across the entire dataset for the given timespan.


## Supported Types

### 

```go
globalValue := components.CreateGlobalValueInteger(int64{/* values here */})
```

### 

```go
globalValue := components.CreateGlobalValueNumber(float64{/* values here */})
```

